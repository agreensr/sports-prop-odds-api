# Docker Compose for Distributed Tracing Backend (Jaeger)
#
# Usage:
#   docker-compose -f docker-compose.tracing.yml up -d
#
# This starts Jaeger all-in-one with:
# - OTLP receiver on port 4317
# - Web UI on port 16686
#
# Set environment variable:
#   export OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

version: '3.8'

services:
  # Jaeger all-in-one - tracing backend
  jaeger:
    image: jaegertracing/all-in-one:latest
    container_name: jaeger
    ports:
      - "4317:4317"   # OTLP gRPC receiver
      - "4318:4318"   # OTLP HTTP receiver
      - "16686:16686" # Jaeger UI
      - "16687:16687" # Jaeger UI (alternative)
      - "14268:14268" # Jaeger collector (HTTP)
      - "14250:14250" # Jaeger collector (gRPC)
      - "9411:9411"   # Zipkin compatible endpoint
    environment:
      - COLLECTOR_OTLP_ENABLED=true
      - SPAN_STORAGE_TYPE=memory
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "localhost:16686"]
      interval: 5s
      timeout: 3s
      retries: 3
    networks:
      - tracing

  # Optional: Grafana Tempo (alternative to Jaeger)
  # tempo:
  #   image: grafana/tempo:latest
  #   container_name: tempo
  #   command:
  #     - "--search-enabled=true"
  #     - "--querier.backend=local"  # Use local storage for demo
  #     - "--querier.local.path=/tmp/tempo"
  #   ports:
  #     - "3200:3200"   # Tempo query port
  #     - "4317:4317"   # OTLP gRPC receiver
  #     - "4318:4318"   # OTLP HTTP receiver
  #     - "9090:9090"   # Tempo query UI
  #   volumes:
  #     - tempo-data:/tmp/tempo
  #   networks:
  #     - tracing

  # Optional: Grafana for Tempo visualization
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: grafana
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_FEATURE_TOGGLES_ENABLE=tempo
  #     - GF_INSTALL_PLUGINS=grafana-tempo-datasource
  #   volumes:
  #     - grafana-data:/var/lib/grafana
  #   networks:
  #     - tracing

networks:
  tracing:
    driver: bridge

# volumes:
#   tempo-data:
#   grafana-data:
